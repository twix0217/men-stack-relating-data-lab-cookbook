<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/stylesheets/style.css">
    <title><%= food.name %></title>
</head>
<body>
    <%- include('../partials/_navbar.ejs') %>
    <h1><%= food.name %></h1>

    <% if (food.quantity) { %>
        <p>Quantity: <%= food.quantity %></p>
    <% } %>

    <% if (food.expirationDate) { %>
        <p>Expiration Date: <%= food.expirationDate.toDateString() %></p>
    <% } %>

    <a href="/users/<%= user._id %>/foods/<%= food._id %>/edit">Edit this food item</a>

    <form 
        action="/users/<%= user._id %>/foods/<%= food._id %>?_method=DELETE" 
        method="post"
    >
        <button type="submit">Delete this food item</button>
    </form>
</body>
</html>